import { Outlet } from "react-router-dom";
// To generate boilerplat react

import React from 'react'

const Layout = () => {
    return (
        <main>
            <Outlet />
        </main>
    )
}

export default Layout // same as filename